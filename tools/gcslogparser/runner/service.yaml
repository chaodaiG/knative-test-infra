apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: gcslogparser-runner-image
  namespace: default
spec:
  runLatest:
    configuration:
      revisionTemplate:
        spec:
          container:
            image: knative.dev/test-infra/tools/gcslogparser/runner